import type React from 'react'

const TerminalCursor: React.FC = () => {
	return (
		<span className="inline-block w-2 h-4 bg-terminal-cyan ml-1 animate-cursor" />
	)
}

export default TerminalCursor
